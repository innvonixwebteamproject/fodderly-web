services:
  fodderly-app-web:
    build:
      context: .
      dockerfile: Dockerfile
    image: fodderlydocker/fodderly-test-web:1.0.29
    container_name: fodderly-test-web
    ports:
      - ${APP_PORT:-3000}:80
    restart: always
    labels:
      project: "fodderly-app-web"
 